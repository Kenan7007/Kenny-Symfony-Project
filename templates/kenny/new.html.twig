{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}Write Your Article{% endblock %}
{% block body %}
    <h1></h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button class="btn btn-success">Publish</button>

    {{form_end(form)}}
{% endblock body %}